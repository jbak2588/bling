import 'package:flutter/material.dart';

class LocalNewsScreen extends StatelessWidget {
  const LocalNewsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('지역소식 화면'));
  }
}
