import 'package:flutter/material.dart';

class LocalStoresScreen extends StatelessWidget {
  const LocalStoresScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('동네업체 화면'));
  }
}
