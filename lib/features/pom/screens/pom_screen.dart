import 'package:flutter/material.dart';

class PomScreen extends StatelessWidget {
  const PomScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('뽐(POM) 화면'),
    );
  }
}