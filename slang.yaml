# slang.yaml
base_locale: id        # ← ko → id 로 변경!!!
fallback_strategy: base_locale
input_directory: assets/lang
input_file_pattern: .json
output_directory: lib/i18n
output_file_name: strings.g.dart
namespaces: false
translate_var: t
key_case: camel